/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.myloview.ru', "picsum.photos", "loremflickr.com", "upload.wikimedia.org"],
    },
}

module.exports = nextConfig
